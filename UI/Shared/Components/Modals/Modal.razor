@inherits Component

@if (IsOpen) {
  <div Class="absolute flex justify-center items-center w-full h-full left-0 top-0 backdrop-blur">
    <Card Class="@Cx("z-10 w-fit h-fit", Class)">
      @ChildContent
    </Card>
  </div>
}

@code {
  [Parameter]
  public bool IsOpen { get; set; }
}
